version: '3.8'

services:
  mcp-server:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NOTION_API_KEY=${NOTION_API_KEY}
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
